We are creating a dummy application

1. Make sure their is a login/registration. No one can acess without it. Use the lovable built-in auth system of course.
2. Once logged in, show a page with the user's name and email address in a dummy UI
3. Also show the "Do Something" button
4. Clicking the button will invoke a server API call
5. The API call will also check if the current user is logged in, if not it will fail
6. If yes, then it will return back "Did something" and the UI will reflect
